# ذاكرة المشروع

## الحالة الحالية

اللعبة تعمل كصفحة ثابتة واحدة على GitHub Pages. تم اختبارها محلياً عبر خادم HTTP، وظهرت شاشة البداية الجديدة، الجولة، شاشة التطوير، والإيقاف المؤقت دون أخطاء في وحدة تحكم المتصفح.

## ملاحظات مهمة

- يجب تشغيلها عبر HTTP أو GitHub Pages، وليس فتح الملف مباشرةً عبر `file://`، لأن بعض المتصفحات تتعامل مع الأصول المحلية بشكل مختلف.
- الحد الأقصى لمقياس Canvas هو `2x` لتقليل تكلفة الرسم على الأجهزة عالية الكثافة.
- `localStorage` يحفظ أعلى نتيجة باسم `tankHS`، مع حماية من فشل التخزين.
- شاشة التطوير تبني بطاقاتها باستخدام DOM آمن، وتقبل النقر أو مفاتيح الأرقام.
- عند إخفاء الصفحة أثناء اللعب، تنتقل اللعبة تلقائياً إلى الإيقاف المؤقت حتى لا يخسر اللاعب تقدمه دون قصد.

## فحوصات منفذة

- استخراج JavaScript من الصفحة وتشغيل `node --check` بنجاح.
- تشغيل `git diff --check` بنجاح.
- فتح اللعبة محلياً والتأكد من عدم وجود أخطاء Console.
- اختبار شاشة التطوير: ظهور 3 اختيارات وتطبيق تطوير `rapid` فعلياً.
- اختبار الإيقاف والاستئناف: انتقال الحالة إلى `paused` ثم العودة إلى `playing`.

## قرارات مؤجلة

- التحديات اليومية ولوحة النتائج العالمية تحتاج seed ثابتاً وواجهة تخزين/خادم، لذلك تم تسجيلها كتوسع تالٍ بدلاً من إدخال اتصال خارجي في الإصدار الثابت الحالي.
- لم تتم إضافة مكتبة رسوم خارجية حتى تبقى اللعبة سريعة وقابلة للعمل من GitHub Pages دون bundler.
